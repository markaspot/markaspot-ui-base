// config/clients/index.ts
// This file exports the client configuration for production.
// For the MUC client, we use the default configuration directly.

import { defaultConfig } from './default';

export default defaultConfig;

// plugins/settings.ts
// Intentionally left minimal: avoid eager form-settings fetch during app init.

/**
 * Settings Plugin
 *
 * Application settings initialization and configuration management.
 *
 * Nuxt plugin for application-wide functionality.
 */

export default defineNuxtPlugin(() => {});

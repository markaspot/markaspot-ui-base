# Suppress false peer dependency warnings for Nuxt 4 compatibility
auto-install-peers=false
strict-peer-dependencies=false

# Fix nested dependency resolution issues (required for @intlify/shared)
shamefully-hoist=true
public-hoist-pattern[]=*